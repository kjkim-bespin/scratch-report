# Cost Report API - 의존성 패키지
# pip install -r requirements.txt

# ── REST API 서버 ──────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ── 스토리지 (S3 / MinIO) ──────────────────────────────────────
boto3>=1.34.0

# ── MD 리포트 생성 스크립트가 추가로 사용하는 패키지 (선택) ────
# psycopg2-binary>=2.9.0   # 향후 psql CLI 없이 직접 DB 접속 시 활성화
